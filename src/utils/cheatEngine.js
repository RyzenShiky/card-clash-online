/**
 * Cheat system DINONAKTIFKAN (production).
 * Secret chat "audio" → panel BGM (lihat chatUI + bgm.js).
 */
export function initCheatEngine() {
    /* no-op */
}

export function tryActivateCheatFromChat() {
    return false;
}

export function resetCheatForRoom() {}
